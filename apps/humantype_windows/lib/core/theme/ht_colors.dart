import 'package:flutter/material.dart';

class HTColors {
  // Background layers
  static const bgBase     = Color(0xFFF3F5F9);   // soft neutral background
  static const bgSurface  = Color(0xFFFFFFFF);   // white card/sidebar
  static const bgElevated = Color(0xFFFFFFFF);   // elevated card

  // Text colors
  static const textPrimary   = Color(0xFF111827); // dark grey
  static const textSecondary = Color(0xFF6B7280); // medium grey
  static const textMuted     = Color(0xFF9CA3AF); // light grey

  // Accent / Status
  static const accentBlue   = Color(0xFF4F46E5);  // primary indigo
  static const accentCyan   = Color(0xFF0EA5E9);  // cyan
  static const accentViolet = Color(0xFF8B5CF6);  // violet
  static const accentGreen  = Color(0xFF10B981);  // emerald (connected)
  static const accentRed    = Color(0xFFEF4444);  // rose (disconnected/error)
  static const accentAmber  = Color(0xFFF59E0B);  // amber

  // Feature tile colors (matching the image)
  static const tileVault     = Color(0xFF8B5CF6);
  static const tileScratch   = Color(0xFFF59E0B);
  static const tileFiles     = Color(0xFF0EA5E9);
  static const tileClipboard = Color(0xFF10B981);
  static const tileNotif     = Color(0xFFF43F5E);
  static const tileHUD       = Color(0xFF8B5CF6);

  // Border & Divider
  static const border = Color(0xFFE5E7EB);
  static const divider = Color(0xFFF3F4F6);
}
